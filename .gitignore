# IDE files
.idea/
.vscode/

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl
terraform/terraform.tfvars
terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.backup
terraform/**/.terraform.lock.hcl
terraform/**/terraform.tfvars
**/terraform.tfvars
# Secret tfvars (values pulled from Secret Manager) - MUST NEVER be committed
**/terraform.tfvars.secret-manager
**/*.secret-manager
**/backend.hcl
**/*.tfplan
**/.terraform/

# Keep .terraform.lock.hcl - it should be committed for version consistency

# Temporary files
*.tmp
*.bak
*~

# OS files
.DS_Store
Thumbs.db

# Claude Code instructions (contains local paths and sensitive info)
CLAUDE.md
.claude/
