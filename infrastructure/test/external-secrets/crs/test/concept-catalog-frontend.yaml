---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: concept-catalog-frontend
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: concept-catalog-frontend
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: concept-catalog-frontend-test
