---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: catalog-comments-service
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: catalog-comments-service
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: catalog-comments-service-test
