---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: dev-commonurl
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: dev-commonurl
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: dev-commonurl-test
