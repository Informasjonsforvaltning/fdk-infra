---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: reference-data
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: reference-data
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: reference-data-test
