---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-informationmodel-harvester
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-informationmodel-harvester
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-informationmodel-harvester-test
