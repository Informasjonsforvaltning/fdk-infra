---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: smtp-certs
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: smtp-certs
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: smtp-certs-test
