---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: catalog-view-api
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: catalog-view-api
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: catalog-view-api-test
