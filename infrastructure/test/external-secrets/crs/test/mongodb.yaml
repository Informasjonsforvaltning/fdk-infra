---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mongodb
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: mongodb
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: mongodb-test
