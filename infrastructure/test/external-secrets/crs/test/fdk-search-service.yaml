---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-search-service
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-search-service
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-search-service-test
