---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: backstage
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: backstage
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: backstage-test
