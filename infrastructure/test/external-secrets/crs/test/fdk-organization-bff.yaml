---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-organization-bff
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-organization-bff
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-organization-bff-test
