---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-dataservice-harvester
  namespace: test
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-dataservice-harvester
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-dataservice-harvester-test
