---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: flux-system
spec:
  values:
    alertmanager:
      ingress:
        enabled: false
    grafana:
      ingress:
        enabled: false
    prometheus:
      ingress:
        enabled: false