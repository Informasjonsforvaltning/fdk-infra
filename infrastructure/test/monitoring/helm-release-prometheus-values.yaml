---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: flux-system
spec:
  values:
    alertmanager:
      ingress:
        hosts:
          - alertmanager.test.fellesdatakatalog.digdir.no
    grafana:
      ingress:
        hosts:
          - grafana.test.fellesdatakatalog.digdir.no
    prometheus:
      ingress:
        hosts:
          - prometheus.test.fellesdatakatalog.digdir.no