---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: thanos
  namespace: flux-system
spec:
  values:
    query:
      ingress:
        enabled: false
    queryFrontend:
      ingress:
        enabled: false
    storegateway:
      ingress:
        enabled: false
    compactor:
      ingress:
        enabled: false