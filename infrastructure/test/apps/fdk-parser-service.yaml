---
# GitRepository for fdk-parser-service
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: fdk-parser-service
  namespace: flux-system
spec:
  interval: 5m
  url: https://github.com/Informasjonsforvaltning/fdk-parser-service
  ref:
    branch: main
---
# Kustomization for fdk-parser-service
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: fdk-parser-service
  namespace: flux-system
spec:
  interval: 5m
  path: ./deploy/prod
  prune: true
  sourceRef:
    kind: GitRepository
    name: fdk-parser-service
  targetNamespace: test
  postBuild:
    substitute:
      GIT_COMMIT_SHA: "latest"