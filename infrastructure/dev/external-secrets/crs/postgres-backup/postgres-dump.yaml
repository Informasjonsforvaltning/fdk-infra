---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-dump
  namespace: postgres-backup
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: postgres-dump
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: postgres-dump-backup