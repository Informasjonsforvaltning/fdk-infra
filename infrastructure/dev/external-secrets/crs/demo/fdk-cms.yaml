---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-cms
  namespace: demo
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-cms
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-cms-demo
