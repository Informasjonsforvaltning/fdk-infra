---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-harvester
  namespace: demo
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-harvester
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-harvester-demo
