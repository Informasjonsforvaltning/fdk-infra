---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-resource-service
  namespace: demo
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-resource-service
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-resource-service-demo
