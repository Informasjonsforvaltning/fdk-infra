---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-reference-data
  namespace: demo
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-reference-data
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-reference-data-demo
