---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-smtp
  namespace: demo
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-smtp
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-smtp-demo
