---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: concept-catalog-frontend
  namespace: staging
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: concept-catalog-frontend
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: concept-catalog-frontend-staging
