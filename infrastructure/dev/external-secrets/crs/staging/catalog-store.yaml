---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: catalog-store
  namespace: staging
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: catalog-store
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: catalog-store-staging
