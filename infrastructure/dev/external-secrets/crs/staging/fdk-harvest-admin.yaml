---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-harvest-admin
  namespace: staging
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-harvest-admin
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-harvest-admin-staging
