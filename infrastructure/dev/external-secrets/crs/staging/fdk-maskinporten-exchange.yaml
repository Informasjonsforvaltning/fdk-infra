---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-maskinporten-exchange
  namespace: staging
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-maskinporten-exchange
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-maskinporten-exchange-staging
