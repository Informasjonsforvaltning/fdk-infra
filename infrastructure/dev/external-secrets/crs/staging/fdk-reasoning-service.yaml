---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-reasoning-service
  namespace: staging
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-reasoning-service
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-reasoning-service-staging
