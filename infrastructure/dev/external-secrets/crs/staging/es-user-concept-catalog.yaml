---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: es-user-concept-catalog
  namespace: staging
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: es-user-concept-catalog
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: es-user-concept-catalog-staging
