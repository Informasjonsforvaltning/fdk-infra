---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mongodb-cluster-init
  namespace: staging
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: mongodb-cluster-init
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: mongodb-cluster-init-staging
