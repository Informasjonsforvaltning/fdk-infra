---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-dataset-rdf-diff-store
  namespace: staging
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-dataset-rdf-diff-store
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-dataset-rdf-diff-store-staging
