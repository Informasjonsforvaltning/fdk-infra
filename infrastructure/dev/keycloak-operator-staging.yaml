# Deploy Keycloak operator in staging environment
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: keycloak-operator-staging
  namespace: flux-system
spec:
  interval: 5m0s
  retryInterval: 1m0s
  path: ./infrastructure/dev/keycloak-operator/keycloak-operator-staging
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
