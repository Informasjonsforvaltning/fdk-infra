---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-community-service
  namespace: prod
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-community-service
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-community-service-prod
