---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-reasoning-service
  namespace: prod
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-reasoning-service
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-reasoning-service-prod
