---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-altinn-proxy
  namespace: prod
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-altinn-proxy
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-altinn-proxy-prod
