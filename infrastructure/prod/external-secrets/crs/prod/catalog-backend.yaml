---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: catalog-backend
  namespace: prod
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: catalog-backend
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: catalog-backend-prod
