---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: fdk-concept-rdf-diff-store
  namespace: prod
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: fdk-concept-rdf-diff-store
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: fdk-concept-rdf-diff-store-prod
