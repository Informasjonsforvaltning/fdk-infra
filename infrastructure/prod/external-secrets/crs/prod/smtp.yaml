---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: smtp
  namespace: prod
spec:
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: smtp
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: smtp-prod
