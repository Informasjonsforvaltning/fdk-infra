---
apiVersion: v1
kind: Service
metadata:
  name: clamav
  namespace: clamav
  labels:
    app: clamav
spec:
  type: ClusterIP
  ports:
    - name: clamd
      port: 3310
      protocol: TCP
      targetPort: 3310
  selector:
    app: clamav
