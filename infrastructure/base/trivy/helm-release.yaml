---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: trivy-operator
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: trivy
  chart:
    spec:
      version: 0.34.x
      chart: trivy-operator
      sourceRef:
        kind: HelmRepository
        name: aquasecurity
      interval: 60m
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
  values:
    excludeNamespaces: "kube-system"
    serviceMonitor:
      enabled: true
      interval: 60s
    trivy:
      ignoreUnfixed: true
      # Use in-memory cache instead of the default BoltDB filesystem cache.
      configFile:
        cache:
          backend: memory
      # Each scan job downloads the ~104 MiB vulnerability DB and, because the
      # cache backend is in-memory, holds it decompressed in RAM alongside the
      # scan working set. 256Mi was not enough for larger images and they were
      # being OOMKilled.
      resources:
        limits:
          cpu: 100m
          memory: 1Gi
        requests:
          cpu: 50m
          memory: 256Mi
    operator:
      vulnerabilityScannerScanOnlyCurrentRevisions: true
      configAuditScannerScanOnlyCurrentRevisions: true
      scanJobTTL: 10m
