---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
patches:
  - patch: |-
      - op: replace
        path: /spec/hostname/hostname
        value: keycloak.fellesdatakatalog.digdir.no
      - op: replace
        path: /spec/hostname/admin
        value: keycloak-admin.fellesdatakatalog.digdir.no
      - op: replace
        path: /spec/instances
        value: 3
      - op: replace
        path: /spec/resources/requests/cpu
        value: 1000m
      - op: replace
        path: /spec/resources/requests/memory
        value: 2Gi
      - op: replace
        path: /spec/resources/limits/cpu
        value: "4"
      - op: replace
        path: /spec/resources/limits/memory
        value: 8Gi
      - op: replace
        path: /spec/ingress/annotations/cert-manager.io~1cluster-issuer
        value: letsencrypt-prod
      - op: add
        path: /spec/additionalOptions/-
        value:
          name: KC_DB_HOST
          secret:
            name: postgres-prod
            key: HOST
      - op: add
        path: /spec/additionalOptions/-
        value:
          name: KC_DB_PORT
          secret:
            name: postgres-prod
            key: PORT
      - op: add
        path: /spec/additionalOptions/-
        value:
          name: KC_DB_DATABASE
          secret:
            name: keycloak
            key: POSTGRESQL_DB
      - op: add
        path: /spec/additionalOptions/-
        value:
          name: KC_DB_USERNAME
          secret:
            name: keycloak
            key: POSTGRESQL_USER
      - op: add
        path: /spec/additionalOptions/-
        value:
          name: KC_DB_PASSWORD
          secret:
            name: keycloak
            key: POSTGRESQL_PASSWORD
    target:
      kind: Keycloak
      name: keycloak
