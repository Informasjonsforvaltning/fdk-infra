---
apiVersion: v1
kind: Service
metadata:
  name: nginx-monitoring
spec:
  selector:
    app: nginx-monitoring
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
