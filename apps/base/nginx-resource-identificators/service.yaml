---
apiVersion: v1
kind: Service
metadata:
  name: nginx-resource-identificators
spec:
  selector:
    app: nginx-resource-identificators
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
