---
apiVersion: v1
kind: Service
metadata:
  name: nginx-misc
spec:
  selector:
    app: nginx-misc
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
