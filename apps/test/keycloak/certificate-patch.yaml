apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-internal-tls
  namespace: test
spec:
  issuerRef:
    name: letsencrypt-test
    kind: ClusterIssuer
  dnsNames:
    - auth.test.fellesdatakatalog.digdir.no