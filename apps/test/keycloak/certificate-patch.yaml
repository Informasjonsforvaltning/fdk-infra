apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-internal-tls
  namespace: test
spec:
  issuerRef:
    name: selfsigned-issuer # Use self-signed issuer for test environment
  dnsNames:
    - keycloak.test.svc.cluster.local